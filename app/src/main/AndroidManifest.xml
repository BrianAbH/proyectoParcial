<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProyectoParcial">
        <!--Actividad Registrar-->
        <activity android:name=".RegistrarActivity" android:windowSoftInputMode="adjustPan"/>
        <!--Actividad Inventario-->
        <activity android:name=".InventarioActivity"/>
        <!--Actividad Detalle-->
        <activity android:name=".DetalleActivity"/>
        <!-- Actividad Reporte -->
        <activity android:name=".ReporteActivity"
            android:exported="false" />
        <!--Actividad Preferencias-->
        <activity android:name=".PreferencesActivity"/>
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>